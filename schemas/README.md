# Public JSON Schemas

These Draft 2020-12 schemas define structural requirements for the clean-room conformance profile.

Cryptographic validity, authorization state, revocation, lineage and non-authority semantics require the protocol rules in addition to schema validation.

A JSON object can be schema-valid and still be semantically invalid. Several invalid vectors intentionally exercise that distinction.
